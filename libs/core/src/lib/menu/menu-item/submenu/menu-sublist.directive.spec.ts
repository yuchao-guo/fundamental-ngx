import { MenuSublistDirective } from './menu-sublist.directive';

describe('MenuSublistDirective', () => {
    it('should create an instance', () => {
        const directive = new MenuSublistDirective();
        expect(directive).toBeTruthy();
    });
});
